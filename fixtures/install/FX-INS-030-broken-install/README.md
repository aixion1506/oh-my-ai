# FX-INS-030 Broken Install

Verifies Doctor identifies dangling managed links after individual repository artifacts disappear. The default read-only Doctor remains compatible with exit 0; strict mode returns exit 1.
