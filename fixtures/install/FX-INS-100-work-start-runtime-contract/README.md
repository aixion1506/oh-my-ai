# FX-INS-100 work-start runtime contract readiness

Verifies that `doctor --strict` validates Claude and Codex Work-start execution
contracts independently, including relative-path regressions and incidental
stable-command text that is not the machine-readable runtime contract.
