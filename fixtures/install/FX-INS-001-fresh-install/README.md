# FX-INS-001 Fresh Install

Verifies a new shared install creates only the expected managed links in an isolated home directory. It also proves `--dry-run` does not render repository instructions or create user configuration paths.
