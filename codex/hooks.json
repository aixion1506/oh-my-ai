{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup|resume|clear",
        "hooks": [
          {
            "type": "command",
            "command": "if [ -x \"$HOME/.local/bin/oh-my-ai\" ]; then \"$HOME/.local/bin/oh-my-ai\" hook codex SessionStart; else cat >/dev/null 2>&1 || :; fi",
            "timeout": 5,
            "statusMessage": "Checking unresolved Context Checkpoints",
            "additionalContextLimit": 1000
          }
        ]
      }
    ],
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "if [ -x \"$HOME/.local/bin/oh-my-ai\" ]; then \"$HOME/.local/bin/oh-my-ai\" hook codex UserPromptSubmit; else cat >/dev/null 2>&1 || :; fi",
            "timeout": 5,
            "statusMessage": "Checking harness routing signals"
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "apply_patch|Bash",
        "hooks": [
          {
            "type": "command",
            "command": "if [ -x \"$HOME/.local/bin/oh-my-ai\" ]; then \"$HOME/.local/bin/oh-my-ai\" hook codex PostToolUse; else cat >/dev/null 2>&1 || :; fi",
            "timeout": 5,
            "statusMessage": "Recording Context Checkpoint activity"
          }
        ]
      }
    ],
    "SessionEnd": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "if [ -x \"$HOME/.local/bin/oh-my-ai\" ]; then \"$HOME/.local/bin/oh-my-ai\" hook codex SessionEnd; else cat >/dev/null 2>&1 || :; fi",
            "timeout": 1
          }
        ]
      }
    ]
  }
}
