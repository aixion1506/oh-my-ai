{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "if [ -x \"$HOME/.local/bin/oh-my-ai\" ]; then \"$HOME/.local/bin/oh-my-ai\" hook claude SessionStart; else cat >/dev/null 2>&1 || :; fi",
            "timeout": 5
          },
          {
            "type": "command",
            "command": "if [ -d \"./docs/context\" ]; then FILES=$(find ./docs/context -name \"*.md\" 2>/dev/null | sort); if [ -n \"$FILES\" ]; then echo \"[HARNESS:context] Available project context:\"; echo \"$FILES\" | sed 's|^|  |'; else echo \"[HARNESS:context] docs/context/ exists but empty — use project-context skill to create\"; fi; fi",
            "timeout": 5
          }
        ]
      }
    ],
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "if [ -x \"$HOME/.local/bin/oh-my-ai\" ]; then \"$HOME/.local/bin/oh-my-ai\" hook claude UserPromptSubmit; else cat >/dev/null 2>&1 || :; fi",
            "timeout": 5
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Skill",
        "hooks": [
          {
            "type": "command",
            "command": "S=$(jq -r '.tool_input.skill // empty' 2>/dev/null); [ -n \"$S\" ] && \"$HOME/.local/bin/harness-event\" emit skill-start --skill \"$S\" --runtime claude; true",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
