interface:
  display_name: "Jira Ticket Create"
  short_description: "중복 검색·승인·반환 검증을 거쳐 Jira 티켓을 안전하게 생성"
  default_prompt: "Use $jira-ticket to validate one Ticket Contract, search for duplicates, preview it, wait for current-preview approval, and create through a connected Jira MCP/Plugin only after approval."
