interface:
  display_name: "Jira Work Gate"
  short_description: "Validate an existing Ticket Contract before planning work"
  default_prompt: "Use $jira-work <ISSUE-KEY> to validate an existing Ticket Contract and preview only the allowed next step."

policy:
  allow_implicit_invocation: false
