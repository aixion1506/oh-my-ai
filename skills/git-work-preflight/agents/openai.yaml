interface:
  display_name: "Git Work Preflight"
  short_description: "Inspect Git state before a future work mutation"
  default_prompt: "Use $git-work-preflight through an approved consumer to inspect Git state without mutations."

policy:
  allow_implicit_invocation: false
